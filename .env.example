# Neon Postgres connection string (required)
DATABASE_URL=

# HTTP Basic Auth credentials enforced by proxy.ts (both required).
# If either is missing the app fails CLOSED and denies all requests.
AUTH_USER=
AUTH_PASSWORD=
